export default function MainPage() {
  // If user has logged in, redirect to the previous board.
  return (
    <div className="flex h-full flex-1 flex-col space-y-8 p-8">
      <h1>Default Board</h1>
    </div>
  );
}
